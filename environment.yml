name: beamRlEnv
channels:
  - conda-forge
  - pytorch
  - nvidia  # optional, for GPU support
  - defaults
dependencies:
  - python=3.9
  - pip
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - scipy
  - jupyter
  - ipython
  - pip:
    - gymnasium>=0.29.1
    - stable-baselines3>=2.0.0
    - torch>=2.0.0
    - holoviews>=1.15.0
    - bokeh>=2.4.0
    - plotly>=5.0.0
    - tensorboard>=2.7.0
    - pillow>=9.0.0
